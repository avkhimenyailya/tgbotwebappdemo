version: "3.3"
services:
  webappjs:
    image: webapp
    build:
      context: happyhacking-react
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
  tgbotjs:
    image: tgbot
    build:
      context: happyhacking-telegraf
      dockerfile: Dockerfile
